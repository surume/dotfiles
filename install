#!/bin/bash
set -euo pipefail

DOTFILES_DIR="${DOTFILES_PATH:-$HOME/src/github.com/surume/dotfiles}"

if [ ! -d "$DOTFILES_DIR" ]; then
  git clone git@github.com:surume/dotfiles.git "$DOTFILES_DIR"
fi

exec "$DOTFILES_DIR/setup"
